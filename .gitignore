# 1) Initialize git (if you haven’t already)
git init

# 2) Add everything
git add .

# 3) Commit
git commit -m "Initial import: model_utils, app, assets, README"

# 4) Link to your GitHub repo (replace with your URL)
git remote add origin https://github.com/YourUser/vascular-classifier.git

# 5) Push to GitHub
git branch -M main
git push -u origin main
